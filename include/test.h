#ifndef TEST_H
#define TEST_H

#include <stdio.h>
#include "color.h"
#include "parse.h"

void	print_color(t_color color);
void	print_t_scene(t_scene *scene);
void	print_t_shape(t_shape *shape);

#endif
